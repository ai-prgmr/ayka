/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: "/ayka-pharma",
  output: "export",
  reactStrictMode: true,
  images: {
    unoptimized: true,
  },
};

module.exports = nextConfig;
